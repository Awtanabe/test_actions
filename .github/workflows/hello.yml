# name: workflowrename
# on: push
# jobs:
#   hello:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           ref: yahooo
#       - run: |
#           ls -a
#           echo ""
#           git branch -a

name: Contextx
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - run echo "${{ github.actor }}"